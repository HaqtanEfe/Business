# Copy to .env.local for local dev (git-ignored), and set the same values in
# your Vercel project's Environment Variables. NEVER commit real secrets.

# Discord webhook the contact form posts to (server-side only, /api/contact).
# Create in Discord: Channel → Edit → Integrations → Webhooks → New Webhook.
DISCORD_WEBHOOK_URL=
